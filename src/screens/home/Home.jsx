/* eslint-disable prettier/prettier */
import { View } from 'react-native';
import { CustomLabel } from '../../components';

const Home = () => {
  return (
    <View style={{backgroundColor: 'red'}}>
     <CustomLabel label="This is the home screen" />
    </View>
  )
}
export {Home};
